{
    "LABEL": {
        "name": ""
    },
    "CHECKS": {
        "regexp": "^(feat|fix|docs|test|ci|chore|build)(\\(.+\\))?!?:\\s(NO-JIRA|[A-Z]+-[0-9]+).*$"
    },
    "MESSAGES": {
        "success": "PR title is valid.",
        "failure": "PR title is invalid.",
        "notice": "PR title needs to match the Conventional Commit standard, scoped to a JIRA ticket or \"NO-JIRA\""
    }
}